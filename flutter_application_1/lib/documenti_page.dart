import 'package:flutter/material.dart';

class DocumentiPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Documenti')),
      body: Center(
        child: Text('Contenuto della pagina dei documenti'),
      ),
    );
  }
}
